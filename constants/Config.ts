/**
 * Central configuration for the application.
 * Update the IP address here to change it across the entire app.
 */
export const Config = {
    // Replace this with your computer's local IP address (e.g., '192.168.1.19')
    // and the port your backend is running on (usually 5000).
    BACKEND_URL: 'http://192.168.1.28:5000',
};

export default Config;
