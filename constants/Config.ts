/**
 * Central configuration for the application.
 * Update the IP address here to change it across the entire app.
 */
export const Config = {
    // Deployed backend URL
    BACKEND_URL: 'https://bill-organizer-backend-1.onrender.com',
};

export default Config;
